# 后端配置，可按自己需求修改（如果某个端口被占用）
HOST=127.0.0.1
PORT=5000
#这是数据库路径，之前有误，可选择注销，注销后系统会强制使用内置的逻辑，自动定位到根目录下的novels.db
DATABASE_URL=sqlite:///novels.db
AUTH_SECRET=change-me-in-production

# AI 模型配置

# 选项: ollama, openai_compat
DEFAULT_PROVIDER=openai_compat

# Ollama 配置 (本地)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5

# OpenAI 兼容 API 配置 (DeepSeek, Moonshot, etc.)
OPENAI_COMPAT_API_KEY=你自己的DeepSeek API Key
OPENAI_COMPAT_BASE_URL=你选择的OpenAI兼容API地址
OPENAI_COMPAT_MODEL=你选择的OpenAI兼容模型
